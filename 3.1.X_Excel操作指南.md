# 3.1.X Excel操作详细步骤指南

## 通用操作流程

### 步骤1：打开Excel文件并查看数据结构

```
1. 打开对应的Excel数据集文件
2. 查看列名，了解数据结构
3. 确认时间列的格式（通常是 "YYYY-MM-DD HH:MM:SS"）
```

---

### 步骤2：添加辅助列 - 提取小时和时间段

#### 2.1 提取小时

在数据表右侧空白列（假设是Z列），添加公式提取小时：

```excel
=HOUR(A2)
```

假设A列是时间列，这个公式会提取小时数（0-23）

#### 2.2 定义时间段

在下一列（AA列），用IF函数定义时间段：

**3.1.1/3.1.2 时间段划分**（06-12, 12-18, 18-24）：
```excel
=IF(Z2>=6 AND Z2<12, "06:00-12:00", IF(Z2>=12 AND Z2<18, "12:00-18:00", "18:00-24:00"))
```

**3.1.5 时间段划分**（06-12, 13-18, 19-05）：
```excel
=IF(AND(Z2>=6,Z2<12),"06:00-12:00",IF(AND(Z2>=12,Z2<18),"13:00-18:00","19:00-05:00"))
```

**3.1.3 时间段划分**（06-08, 17-20, 其余）：
```excel
=IF(AND(Z2>=6,Z2<8),"06:00-08:00",IF(AND(Z2>=17,Z2<20),"17:00-20:00","其余时段"))
```

---

### 步骤3：使用数据透视表分析

#### 3.1 插入数据透视表

```
1. 选中整个数据区域（Ctrl+A）
2. 点击"插入" → "数据透视表"
3. 选择"新工作表"
4. 点击"确定"
```

#### 3.2 配置数据透视表

**场景A：按时间段统计平均值**（3.1.2/3.1.5）

```
行区域：时间段列（AA列）
值区域：温度、湿度、光照等（设置为"平均值"）
```

操作步骤：
1. 将"时间段"字段拖到"行"区域
2. 将"温度"字段拖到"值"区域
3. 点击值字段设置 → 选择"平均值"
4. 重复步骤2-3，添加湿度、光照等

**场景B：统计功能使用频率**（3.1.1）

```
行区域：功能列
值区域：功能列（设置为"计数"）
排序：降序排列
```

操作步骤：
1. 将"功能调用类型"拖到"行"区域
2. 再将"功能调用类型"拖到"值"区域
3. 右键点击计数结果 → 排序 → 降序

**场景C：按功能统计平均响应时间**

```
行区域：功能列
值区域：响应时间（设置为"平均值"）
排序：降序排列
```

---

### 步骤4：使用Excel函数直接计算

#### 4.1 AVERAGEIFS函数（多条件平均）

**语法**：
```excel
=AVERAGEIFS(平均区域, 条件区域1, 条件1, 条件区域2, 条件2, ...)
```

**示例1：计算06:00-12:00的平均温度**
```excel
=AVERAGEIFS(温度列, 时间段列, "06:00-12:00")
```

**示例2：计算"调整音量"功能的平均响应时间**
```excel
=AVERAGEIFS(响应时间列, 功能列, "调整音量")
```

#### 4.2 COUNTIF函数（计数）

**语法**：
```excel
=COUNTIF(条件区域, 条件)
```

**示例：统计"调整音量"功能的使用次数**
```excel
=COUNTIF(功能列, "调整音量")
```

#### 4.3 AVERAGE函数（简单平均）

**示例：计算整体平均响应时间**
```excel
=AVERAGE(响应时间列)
```

---

## 各题目具体操作步骤

### 3.1.1 智能音箱产品

#### 任务1：用户使用习惯 - 最常被使用的功能

**方法1：数据透视表**
```
1. 插入数据透视表
2. 行：功能调用类型
3. 值：功能调用类型（计数）
4. 排序：降序
5. 取前3个
```

**方法2：COUNTIF函数**
```excel
=COUNTIF(B:B, "调整音量")
=COUNTIF(B:B, "查询新闻")
=COUNTIF(B:B, "查天气")
...对每个功能计数后比较
```

**预期答案**：调整音量、查询新闻、查天气

---

#### 任务2：功能使用频率

**最受欢迎**：计数最多的功能
**较少使用**：计数最少的2个功能

**数据透视表操作**：
```
1. 行：功能调用类型
2. 值：计数
3. 排序后查看最高和最低
```

**预期答案**：
- 最受欢迎：调整音量
- 较少使用：播放音乐、控制家居

---

#### 任务3：响应时间分析

**数据透视表操作**：
```
1. 行：功能调用类型
2. 值：响应时间（平均值）
3. 排序：降序
```

**预期答案**：
- 响应时间较长：控制家居
- 响应时间适中：查询知识、调整音量、提醒事项
- 响应时间较短：查询新闻

---

### 3.1.2 智能照明系统

#### 任务1：用户使用习惯 - 按时间段统计

**步骤**：
1. 添加时间段列（06-12, 12-18, 18-24）
2. 数据透视表：
   - 行：时间段
   - 值：光线亮度（平均值）、色温（平均值）

**预期答案**：
| 时间段 | 平均亮度 | 平均色温 |
|--------|---------|---------|
| 06:00-12:00 | 51.74 | 3689.52 |
| 12:00-18:00 | 49.93 | 3732.40 |
| 18:00-24:00 | 48.06 | 3661.35 |

---

#### 任务2：智能场景使用频率

**数据透视表**：
```
行：智能场景
值：计数
排序：降序
```

**预期答案**：
- 频繁使用：Relax Mode
- 适中使用：Reading Mode, Work Mode
- 较少使用：Sleep Mode

---

#### 任务3：响应时间分析

**计算平均响应时间**：
```excel
=AVERAGE(响应时间列)
```

**预期答案**：
- 平均响应时间：1.06秒
- 延迟瓶颈：网络延迟、系统处理能力

---

### 3.1.3 智能健康手环

#### 任务1：用户活动模式

**步骤**：
1. 添加时间段列（06-08, 17-20, 其余）
2. 数据透视表：
   - 行：时间段
   - 值：步数（平均值）

**预期答案**：
| 时间段 | 平均步数 |
|--------|---------|
| 06:00-08:00 | 5068.10 |
| 17:00-20:00 | 5005.65 |
| 其余时段 | 1101.25 |

---

#### 任务2：健康指标关注度

**方法**：比较各指标的数据完整度或查看次数

**预期答案**：
- 最受关注：步数
- 较少关注：心率、睡眠时长

---

#### 任务3：数据同步性能

**计算平均延迟**：
```excel
=AVERAGE(同步延迟列)
```

**预期答案**：
- 平均延迟：0.70秒
- 影响因素：蓝牙连接稳定性、手环与手机距离

---

### 3.1.4 智能健康监测系统

#### 任务1：用户活动周期

**步骤**：
1. 提取小时列
2. 分析各时段血压、血糖变化

**预期答案**：
- 血压在早晨（06:00-08:00）有明显上升趋势
- 血糖在进餐后（07:00-09:00、12-14、18-20）显著升高
- 高风险时段：06:00-22:00
- 安全时段：22:00-06:00

---

#### 任务2：健康指标偏好度

**方法**：统计各指标的数据量
```excel
=COUNTA(收缩压列)  // 血压监测
=COUNTA(血糖值列)  // 血糖检测
=COUNTA(体脂值列)  // 体脂分析
```

**预期答案**：
- 受青睐：血压监测、血糖检测
- 较少使用：体脂分析

---

#### 任务3：系统响应与准确性

**数据透视表**：
```
行：功能类型
值：响应时间（平均值）
排序：降序
```

**预期答案**：
- 响应时间较长：体脂分析
- 响应时间适中：血压监测
- 响应时间较短：血糖检测

---

### 3.1.5 智能家居环境控制系统

#### 任务1：用户环境偏好

**步骤**：
1. 添加时间段列（06-12, 13-18, 19-05）
2. 数据透视表：
   - 行：时间段
   - 值：温度（平均值）、湿度（平均值）、光照（平均值）

**预期答案**：
| 时间段 | 平均温度 | 平均湿度 | 平均光照 |
|--------|---------|---------|---------|
| 06:00-12:00 | 24.72 | 50.18 | 540.94 |
| 13:00-18:00 | 24.92 | 49.08 | 557.80 |
| 19:00-05:00 | 25.02 | 49.92 | 542.75 |

---

#### 任务2：系统响应时间

**计算平均响应时间**：
```excel
=AVERAGE(响应时间列)
```

**预期答案**：
- 平均响应时间：3.02秒
- 影响因素：网络延迟、系统处理能力

---

#### 任务3：能源消耗分析

**计算平均能耗**：
```excel
=AVERAGE(能源消耗列)
```

**预期答案**：
- 平均能源消耗：1.02
- 节能潜力：
  - 优化算法减少不必要操作
  - 预测模型避免频繁启停

---

## Excel快捷键速查

| 操作 | 快捷键 |
|------|--------|
| 插入数据透视表 | Alt → N → V |
| 自动求和 | Alt + = |
| 填充公式 | Ctrl + D |
| 选中整列 | Ctrl + Space |
| 选中整行 | Shift + Space |
| 快速打开函数 | Shift + F3 |
| 排序 | Alt → A → S → S |
| 筛选 | Ctrl + Shift + L |

---

## 验证答案技巧

### 1. 交叉验证
- 用数据透视表算一次
- 用AVERAGEIFS函数再算一次
- 对比结果是否一致

### 2. 检查时间段划分
- 确认小时提取正确
- 确认时间段边界正确（如6<=hour<12）
- 检查是否有数据遗漏

### 3. 排序验证
- 功能频率：降序排列后检查前3个
- 响应时间：降序排列后分长/中/短

### 4. 数值精度
- 答案通常保留2位小数
- 使用ROUND函数：`=ROUND(AVERAGE(...), 2)`

---

## 常见问题

### Q1: 时间列格式不对怎么办？
A: 先转换格式：
```
1. 选中时间列
2. 右键 → 设置单元格格式
3. 选择"日期"或"自定义"
4. 或使用函数：=DATEVALUE(LEFT(A2,10))
```

### Q2: 数据透视表不更新？
A: 右键点击透视表 → 刷新

### Q3: 如何快速复制公式到整列？
A: 双击单元格右下角的小方块（填充柄）

### Q4: 如何按条件筛选后统计？
A: 使用筛选功能（Ctrl+Shift+L），筛选后状态栏会显示计数和平均值