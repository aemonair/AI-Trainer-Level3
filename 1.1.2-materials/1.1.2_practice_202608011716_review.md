# 1.1.2 练习 Review - 202608011716

## 练习文件
[1.1.2_practice_202608011716.ipynb](file:///Users/air/Downloads/GUIDE_AI_3/人工智能训练师_3级_sucai/1.1.2-素材/1.1.2_practice_202608011716.ipynb)

---

## ✅ 完成情况

### 任务1：传感器数据统计 ✅
- **代码详解**：`data.groupby('SensorType')['Value'].agg(['count', 'mean'])`
- **你的代码**：`data.groupby('SensorType')['Value'].agg(('count','mean'))`
- **结果**：正确统计了5种传感器类型的数量和平均值
- **差异**：使用元组 `()` 而非列表 `[]`，结果相同 ✅

### 任务2：按位置统计温度和湿度 ✅
- **代码详解**：`data[data['SensorType'].isin(['Temperature', 'Humidity'])].groupby(['Location', 'SensorType'])['Value'].mean().unstack()`
- **你的代码**：完全一致 ✅
- **结果**：正确计算了4个位置的温度和湿度平均值

### 任务3：数据清洗和异常值处理 ✅
- **标记异常值**：与代码详解完全一致 ✅
- **fillna**：`fillna(method = 'bfill')` - 比代码详解更规范（明确写了 `method=`）✅
- **drop列**：`data.drop(columns=['is_abnormal'])` - 正确 ✅
- **结果**：异常值数量1011，保存成功 ✅

---

## 📊 与代码详解对比

| 任务 | 匹配度 | 说明 |
|------|--------|------|
| 任务1 | 95% | 元组vs列表，结果相同 |
| 任务2 | 100% | 完全一致 |
| 任务3 | 100% | 完全一致，且fillna写法更规范 |

---

## ❌ 历史错误分析

### 第一次练习（16:59）- 2个错误

| 错误行 | 错误代码 | 错误类型 | 正确写法 |
|--------|---------|---------|---------|
| L14-15 | `fillna('bfill', inplace=True)` | **参数位置错误** | `fillna(method='bfill', inplace=True)` |
| L17 | `data(data=['is_abnormal'])` | **对象调用错误** | `data.drop(columns=['is_abnormal'])` |

### 第二次练习（17:16）- 1个错误

| 错误行 | 错误代码 | 错误类型 | 正确写法 |
|--------|---------|---------|---------|
| L15 | `fillna(methos = 'ffill')` | **拼写错误** | `fillna(method = 'ffill')` |

---

## 📝 错误模式总结

| 错误类型 | 出现次数 | 严重程度 | 改进建议 |
|---------|---------|---------|---------|
| 参数写法错误 | 2次 | 中等 | 使用关键字参数 `method=` 更清晰 |
| 对象调用错误 | 1次 | 高 | DataFrame不是函数，用 `.method()` |
| 拼写错误 | 1次 | 低 | 注意IDE红色波浪线提示 |

---

## 📊 评分估算

| 任务 | 满分 | 得分 | 说明 |
|------|------|------|------|
| 传感器数据统计 | 3 | 3 | ✅ 完全正确 |
| 按位置统计 | 2 | 2 | ✅ 完全正确 |
| 标记异常值 | 3 | 3 | ✅ 完全正确 |
| 输出异常值数量 | 2 | 2 | ✅ 完全正确 |
| 填补缺失值 | 4 | 4 | ✅ 已修正 |
| 删除列并保存 | 4 | 4 | ✅ 已修正 |
| **总计** | **18** | **18** | **100%** ✅ |

---

## 💡 学习建议

1. **参数写法**：使用关键字参数（如 `method='bfill'`）比位置参数更清晰
2. **对象调用**：记住 DataFrame 不是函数，使用 `.method()` 而不是 `()`
3. **拼写检查**：IDE 会自动提示，注意红色波浪线
4. **重复练习**：1.1.2 练习了4次才完全掌握，说明这类题目需要多练