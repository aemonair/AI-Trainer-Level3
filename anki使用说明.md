# 使用说明

## 一、Anki卡片导入指南

### 1. 导入步骤

1. 打开Anki桌面版
2. 点击 **文件** → **导入**
3. 选择 `anki_cards.csv` 文件
4. 设置导入选项：
   - **字段分隔符**: 分号（`;`）
   - **允许HTML**: 勾选
   - **标签**: 可选填写（如 `人工智能训练师`）
5. 点击 **导入**

### 2. 卡片分类

导入的卡片按章节分类：
- **1.2.X**: 业务模块优化（5题）
- **3.1.X**: 智能产品数据分析（5题）
- **4.1.X**: 培训大纲编写（5题）
- **4.2.X**: 数据采集和处理指导（5题）

### 3. 学习建议

- **正面**: 题目/问题
- **背面**: 标准答案要点
- 建议每天复习10-15张卡片
- 重点记忆数值和关键词

---

## 二、Python代码模板使用指南

### 1. 环境准备

```bash
# 安装依赖
pip install pandas openpyxl python-docx numpy
```

### 2. 使用步骤

#### 步骤1: 修改配置

打开 `3.1.X_数据分析模板.py`，修改以下配置：

```python
# 修改文件路径
FILE_PATH = 'template/3.1.5/智能家居环境控制系统数据集.xlsx'

# 修改章节号
CHAPTER = '3.1.5'  # 可选: 3.1.1 ~ 3.1.5
```

#### 步骤2: 运行脚本

```bash
python "3.1.X_数据分析模板.py"
```

#### 步骤3: 查看结果

- 控制台会输出分析结果
- 自动生成Word报告：`output/3.1.5-1.docx`

### 3. 各题目快速切换

只需修改两行代码：

```python
# 3.1.1 智能音箱
FILE_PATH = 'template/3.1.1/智能音箱数据集.xlsx'
CHAPTER = '3.1.1'

# 3.1.2 智能照明
FILE_PATH = 'template/3.1.2/智能照明系统数据集.xlsx'
CHAPTER = '3.1.2'

# 3.1.3 健康手环
FILE_PATH = 'template/3.1.3/智能健康手环数据集.xlsx'
CHAPTER = '3.1.3'

# 3.1.4 健康监测
FILE_PATH = 'template/3.1.4/智能健康监测系统数据集.xlsx'
CHAPTER = '3.1.4'

# 3.1.5 智能家居
FILE_PATH = 'template/3.1.5/智能家居环境控制系统数据集.xlsx'
CHAPTER = '3.1.5'
```

### 4. 核心代码速记

考试时可能需要手写的核心代码：

```python
import pandas as pd

# 1. 读取Excel
df = pd.read_excel('数据文件.xlsx')

# 2. 提取小时
df['hour'] = pd.to_datetime(df['时间']).dt.hour

# 3. 定义时间段
def get_period(hour):
    if 6 <= hour < 12:
        return '06:00 - 12:00'
    elif 12 <= hour < 18:
        return '13:00 - 18:00'
    else:
        return '19:00 - 05:00'

df['period'] = df['hour'].apply(get_period)

# 4. 按时间段统计
stats = df.groupby('period')[['温度', '湿度', '光照']].mean()

# 5. 平均值
avg_value = df['列名'].mean()

# 6. 频率统计
freq = df['功能列'].value_counts()

# 7. 分组统计
group_stats = df.groupby('功能列')['响应时间'].mean()
```

---

## 三、Excel操作替代方案

如果考试不允许使用Python，可以用Excel函数：

| 操作 | Excel公式 | 示例 |
|------|----------|------|
| 平均值 | `=AVERAGE(B2:B100)` | 计算平均温度 |
| 条件平均 | `=AVERAGEIF(A2:A100, "06:00-12:00", B2:B100)` | 按时段平均 |
| 计数 | `=COUNTIF(C2:C100, "调整音量")` | 功能使用次数 |
| 排序 | 数据 → 排序 | 按响应时间排序 |
| 透视表 | 插入 → 数据透视表 | 多维度分析 |

---

## 四、复习策略

### 1. 记忆优先级

**高优先级**（必考）:
- 3.1.X 的具体数值（平均温度、响应时间等）
- 优化方向和解决方案的关键词

**中优先级**:
- 1.2.X 的问题类型和影响
- 4.1.X 的培训大纲结构

**低优先级**:
- 4.2.X 的具体操作步骤

### 2. 记忆技巧

- **数值类**: 记住大致范围即可（如温度24-25℃）
- **优化方向**: 记住关键词（性能、个性化、效率）
- **解决方案**: 记住核心方法（算法优化、硬件升级、用户体验）

### 3. 每日复习计划

| 时间 | 内容 | 卡片数 |
|------|------|--------|
| 早晨 | 3.1.X 数据分析 | 10张 |
| 中午 | 1.2.X 业务优化 | 10张 |
| 晚上 | 4.1.X + 4.2.X | 10张 |

---

## 五、常见问题

### Q1: Anki导入后格式混乱？
A: 确保导入时选择**分号**作为分隔符，并勾选**允许HTML**。

### Q2: Python脚本运行报错？
A: 检查：
1. 是否安装了依赖包（pandas, openpyxl, python-docx）
2. 文件路径是否正确
3. Excel文件是否存在

### Q3: 考试时能带电脑吗？
A: 根据考试要求，如果不能使用电脑，建议：
- 重点记忆Anki卡片中的关键数值
- 手写练习核心Python代码
- 熟记Excel操作步骤

---

## 六、文件清单

| 文件 | 用途 |
|------|------|
| `anki_cards.csv` | Anki卡片导入文件 |
| `3.1.X_数据分析模板.py` | Python数据分析模板 |
| `使用说明.md` | 本文件 |

---

**祝考试顺利！** 🎯