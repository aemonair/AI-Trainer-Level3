# 【考前结论】

**代码题总体掌握度**: 92.3% (622/674)
- 已掌握任务: 19个
- 薄弱任务: 12个
- 总任务类型: 38个

**P0必须解决**:
1. 图像resize参数顺序
2. 数据保存变量名匹配
3. 准确率计算原理
4. ONNX输入构造
5. Top-K用argsort

**P1应该解决**:
1. pd.cut的right=False
2. groupby+apply计算比例
3. fillna的method参数
4. np.where多条件

**已经掌握**:
- pd.read_csv, data.groupby, pd.cut, data.astype
- ONNX推理, np.array, Image.open
- data.value_counts, data.between, data.isin

**不需要再刷**:
- 所有100%正确率且出现10次以上的API

**最值得投入时间的3件事情**:
1. 图像resize参数顺序 - 考试必考，已错2次
2. 数据保存变量名匹配 - 已错6次，都是粗心
3. 准确率计算原理 - 理解(y_test==y_pred).mean()