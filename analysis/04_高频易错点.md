# ⚠️ 高频易错点分析

生成时间: 2026-08-23 19:57:54


### 数据保存
- **错误类型**: 变量名错误
- **错误次数**: 4/27
- **错误代码**: `cleaned_data.to_csv(...) 但实际变量名是data_cleaned`
- **正确代码**: `确保变量名与题目中的一致`
- **原因**: 填空题中变量名必须与上下文匹配
- **修正**: 仔细看题目中的变量名，不要自己改名
- 实际错误: `data_cleaned.to_csv(cleaned_file_path, index=False)`
- 实际错误: `data_cleaned.to_csv(cleaned_file_path, index=False)`

### 数据统计
- **错误类型**: 粗心错误
- **错误次数**: 2/2
- **错误代码**: `initial_row_count = len(data) 但data还未定义或已修改`
- **正确代码**: `确保在正确的时机调用len(data)`
- **原因**: 代码执行顺序问题，或变量名错误
- **修正**: 注意代码执行顺序，确保变量已定义
- 实际错误: `initial_row_count = len(data)   #处理前的数据行数`
- 实际错误: `final_row_count = len(data)     #处理后的数据行数`

### 列删除
- **错误类型**: 参数错误
- **错误次数**: 2/8
- **错误代码**: `data.drop(columns=[...]) 变量名或列名错误`
- **正确代码**: `data.drop(columns=['Unnamed: 0', target_variable])`
- **原因**: 列名要准确匹配，变量名要与题目一致
- **修正**: 仔细检查列名拼写和变量名
- 实际错误: `X = data.drop(columns=['Unnamed: 0', target_variable])   #1分`
- 实际错误: `X = data.drop(columns=['Unnamed: 0', target_variable])   #1分`

### 准确率计算
- **错误类型**: 概念错误
- **错误次数**: 2/8
- **错误代码**: `train_score = (x_test == y_pred).mean() 变量名错误`
- **正确代码**: `(y_test == y_pred).mean()`
- **原因**: 不理解(y_test == y_pred).mean()的原理：布尔值True=1, False=0
- **修正**: 记住：比较返回布尔Series，.mean()计算True的比例
- 实际错误: `train_score = (x_test == y_pred).mean()   #训练集分数`
- 实际错误: `test_score = (y_test==y_pred).mean()    #测试集分数`

### 维度扩展
- **错误类型**: API使用错误
- **错误次数**: 1/10
- **错误代码**: `np.expand_dims()[0].name`
- **正确代码**: `ort_session.get_inputs()[0].name`
- **原因**: 混淆了expand_dims和get_inputs的用途
- **修正**: get_inputs()获取模型输入信息，expand_dims()扩展数组维度
- 实际错误: `ort_inputs = {np.expand_dims()[0].name: image_array}`

### Top-K预测
- **错误类型**: 流程错误
- **错误次数**: 1/6
- **错误代码**: `np.argmax(probabilities[0])[-5:][::-1]`
- **正确代码**: `np.argsort(probabilities[0])[-5:][::-1]`
- **原因**: argmax返回单个索引，argsort返回排序后的索引数组
- **修正**: Top-K用argsort，不是argmax
- 实际错误: `top5_idx = np.argmax(probabilities[0])[-5:][::-1]`